# Streams

Your assay tracking streams live here — one directory per stream, each holding its
briefs and a README register.

This tutorial ships with **no streams yet**: create your first one to begin. A fresh,
stream-less skeleton is expected, so CI runs the lint with an empty-root allowance
until you add content.
